{
  "name": "snapog",
  "version": "0.3.0",
  "description": "OG image generation API — fast, simple, Cloudflare Workers + Hono + satori",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate": "wrangler d1 migrations apply snapog-db"
  },
  "dependencies": {
    "@resvg/resvg-wasm": "^2.6.2",
    "hono": "^4.6.0",
    "satori": "^0.12.1",
    "stripe": "^22.3.2"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.0.0"
  }
}
